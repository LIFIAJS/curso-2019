import React from "react";
import ReactDOM from "react-dom";

const rootElement = document.getElementById("root");
ReactDOM.render(<h2>Para probar, importe y reemplaze el h2 por la componente</h2>, rootElement);

